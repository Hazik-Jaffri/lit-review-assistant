# Copy this file to .env if you want to set the key via environment variable
# instead of typing it into the Streamlit sidebar each time (optional --
# the app's sidebar input always works and does not require this file).
#
# Get a free key at: https://aistudio.google.com/apikey
GEMINI_API_KEY= your_api_key_here
